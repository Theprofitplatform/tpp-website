---
import BaseLayout from '../layouts/BaseLayout.astro';

const title = "Portfolio | The Profit Platform - Success Stories & Case Studies";
const description = "Explore our portfolio of successful digital marketing campaigns. Real results from 200+ projects with 280% average ROI.";

const projects = [
  {
    title: "E-commerce Revenue Boost",
    client: "Fashion Retailer",
    location: "Sydney CBD",
    service: "Google Ads",
    image: "🛍️",
    results: [
      { metric: "Revenue Increase", value: "+385%" },
      { metric: "ROAS", value: "12:1" },
      { metric: "Conversion Rate", value: "+156%" }
    ]
  },
  {
    title: "Local SEO Domination",
    client: "Dental Practice",
    location: "North Shore",
    service: "SEO",
    image: "🦷",
    results: [
      { metric: "Organic Traffic", value: "+420%" },
      { metric: "Local Rankings", value: "#1" },
      { metric: "New Patients", value: "+65/mo" }
    ]
  },
  {
    title: "B2B Lead Generation",
    client: "SaaS Company",
    location: "Surry Hills",
    service: "Google Ads",
    image: "💼",
    results: [
      { metric: "Cost per Lead", value: "-72%" },
      { metric: "Lead Quality", value: "+89%" },
      { metric: "Pipeline Value", value: "$2.4M" }
    ]
  },
  {
    title: "Website Redesign Success",
    client: "Law Firm",
    location: "Parramatta",
    service: "Web Design",
    image: "⚖️",
    results: [
      { metric: "Page Speed", value: "+94%" },
      { metric: "Bounce Rate", value: "-61%" },
      { metric: "Inquiries", value: "+220%" }
    ]
  },
  {
    title: "Restaurant Chain Growth",
    client: "Hospitality Group",
    location: "Eastern Suburbs",
    service: "SEO",
    image: "🍽️",
    results: [
      { metric: "Bookings", value: "+178%" },
      { metric: "Brand Searches", value: "+340%" },
      { metric: "Revenue", value: "+$1.2M" }
    ]
  },
  {
    title: "Real Estate Leads",
    client: "Property Agency",
    location: "Inner West",
    service: "Google Ads",
    image: "🏠",
    results: [
      { metric: "Qualified Leads", value: "+290%" },
      { metric: "Cost per Acquisition", value: "-58%" },
      { metric: "Properties Sold", value: "+45" }
    ]
  }
];

const testimonials = [
  {
    text: "The Profit Platform transformed our online presence. We've seen incredible growth in both traffic and revenue.",
    author: "Sarah Mitchell",
    role: "CEO, Fashion Retailer",
    rating: 5
  },
  {
    text: "Best investment we've made. Our ROI has exceeded all expectations, and the team is fantastic to work with.",
    author: "Dr. James Chen",
    role: "Director, Dental Practice",
    rating: 5
  },
  {
    text: "Professional, responsive, and results-driven. They've helped us dominate our local market.",
    author: "Michael Torres",
    role: "Founder, Tech Startup",
    rating: 5
  }
];
---

<BaseLayout title={title} description={description}>
  <style>
    .hero {
      background: linear-gradient(135deg, #8b5cf6 0%, #3b82f6 100%);
      padding: 120px 0 80px;
      text-align: center;
      color: white;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      font-weight: 900;
      margin-bottom: 20px;
    }

    .hero-stats {
      display: flex;
      justify-content: center;
      gap: 60px;
      margin-top: 40px;
      flex-wrap: wrap;
    }

    .hero-stat {
      text-align: center;
    }

    .stat-value {
      font-size: 2.5rem;
      font-weight: 900;
    }

    .stat-label {
      opacity: 0.9;
      font-size: 1.1rem;
    }

    .filter-section {
      padding: 40px 0;
      text-align: center;
      background: white;
      border-bottom: 1px solid #e2e8f0;
    }

    .filter-buttons {
      display: flex;
      justify-content: center;
      gap: 15px;
      flex-wrap: wrap;
    }

    .filter-btn {
      padding: 10px 25px;
      background: white;
      border: 2px solid #e2e8f0;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .filter-btn:hover,
    .filter-btn.active {
      background: linear-gradient(135deg, #8b5cf6, #3b82f6);
      color: white;
      border-color: transparent;
    }

    .portfolio-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 40px;
      padding: 80px 0;
    }

    .project-card {
      background: white;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
    }

    .project-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.12);
    }

    .project-image {
      height: 200px;
      background: linear-gradient(135deg, #e0e7ff 0%, #cdd5f4 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 4rem;
    }

    .project-content {
      padding: 30px;
    }

    .project-title {
      font-size: 1.5rem;
      font-weight: 700;
      color: #1e293b;
      margin-bottom: 10px;
    }

    .project-meta {
      display: flex;
      gap: 15px;
      margin-bottom: 20px;
      flex-wrap: wrap;
    }

    .project-tag {
      display: inline-block;
      padding: 5px 12px;
      background: #f1f5f9;
      color: #64748b;
      border-radius: 15px;
      font-size: 0.9rem;
    }

    .project-results {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
      margin-top: 25px;
      padding-top: 25px;
      border-top: 1px solid #e2e8f0;
    }

    .result-item {
      text-align: center;
    }

    .result-value {
      font-size: 1.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, #8b5cf6, #3b82f6);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .result-label {
      font-size: 0.85rem;
      color: #64748b;
      margin-top: 5px;
    }

    .testimonials-section {
      background: #f8fafc;
      padding: 80px 0;
    }

    .testimonials-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 30px;
      margin-top: 60px;
    }

    .testimonial-card {
      background: white;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.06);
    }

    .testimonial-text {
      font-size: 1.1rem;
      line-height: 1.8;
      color: #475569;
      margin-bottom: 30px;
    }

    .testimonial-author {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .author-avatar {
      width: 50px;
      height: 50px;
      background: linear-gradient(135deg, #8b5cf6, #3b82f6);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 700;
      font-size: 1.2rem;
    }

    .author-info {
      flex: 1;
    }

    .author-name {
      font-weight: 700;
      color: #1e293b;
    }

    .author-role {
      color: #64748b;
      font-size: 0.9rem;
    }

    .rating {
      color: #fbbf24;
      font-size: 1.2rem;
    }

    @media (max-width: 768px) {
      .portfolio-grid {
        grid-template-columns: 1fr;
      }

      .testimonials-grid {
        grid-template-columns: 1fr;
      }

      .project-results {
        grid-template-columns: 1fr;
        gap: 10px;
      }
    }
  </style>

  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <h1>Our Success Stories</h1>
      <p style="font-size: 1.25rem; max-width: 600px; margin: 0 auto; opacity: 0.95;">
        Real results from real businesses. See how we've helped companies like yours achieve exceptional growth.
      </p>
      <div class="hero-stats">
        <div class="hero-stat">
          <div class="stat-value">200+</div>
          <div class="stat-label">Projects Completed</div>
        </div>
        <div class="hero-stat">
          <div class="stat-value">280%</div>
          <div class="stat-label">Average ROI</div>
        </div>
        <div class="hero-stat">
          <div class="stat-value">4.9★</div>
          <div class="stat-label">Client Rating</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Filter Section -->
  <section class="filter-section">
    <div class="container">
      <div class="filter-buttons">
        <button class="filter-btn active">All Projects</button>
        <button class="filter-btn">Web Design</button>
        <button class="filter-btn">SEO</button>
        <button class="filter-btn">Google Ads</button>
      </div>
    </div>
  </section>

  <!-- Portfolio Grid -->
  <section class="container">
    <div class="portfolio-grid">
      {projects.map((project) => (
        <div class="project-card">
          <div class="project-image">
            {project.image}
          </div>
          <div class="project-content">
            <h3 class="project-title">{project.title}</h3>
            <div class="project-meta">
              <span class="project-tag">📍 {project.location}</span>
              <span class="project-tag" style="background: linear-gradient(135deg, #e0e7ff, #c7d2fe); color: #6366f1;">
                {project.service}
              </span>
            </div>
            <p style="color: #64748b; margin-bottom: 20px;">{project.client}</p>
            <div class="project-results">
              {project.results.map((result) => (
                <div class="result-item">
                  <div class="result-value">{result.value}</div>
                  <div class="result-label">{result.metric}</div>
                </div>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Testimonials Section -->
  <section class="testimonials-section">
    <div class="container">
      <h2 class="gradient-text" style="text-align: center; font-size: 2.5rem; margin-bottom: 20px;">
        What Our Clients Say
      </h2>
      <p style="text-align: center; color: #64748b; font-size: 1.2rem; max-width: 600px; margin: 0 auto;">
        Don't just take our word for it - hear from businesses we've helped grow
      </p>

      <div class="testimonials-grid">
        {testimonials.map((testimonial) => (
          <div class="testimonial-card">
            <div class="rating">
              {Array(testimonial.rating).fill('★').join('')}
            </div>
            <p class="testimonial-text">"{testimonial.text}"</p>
            <div class="testimonial-author">
              <div class="author-avatar">
                {testimonial.author.split(' ').map(n => n[0]).join('')}
              </div>
              <div class="author-info">
                <div class="author-name">{testimonial.author}</div>
                <div class="author-role">{testimonial.role}</div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section style="background: linear-gradient(135deg, #1e293b, #334155); padding: 100px 0; text-align: center; color: white;">
    <div class="container">
      <h2 style="font-size: 3rem; margin-bottom: 20px; font-weight: 900;">Ready to Be Our Next Success Story?</h2>
      <p style="font-size: 1.3rem; opacity: 0.9; margin-bottom: 40px; max-width: 600px; margin-left: auto; margin-right: auto;">
        Let's discuss how we can transform your digital presence and drive real results
      </p>
      <a href="/contact" class="btn btn-primary" style="font-size: 1.2rem; padding: 20px 40px;">
        Start Your Project →
      </a>
    </div>
  </section>

  <script>
    // Filter functionality
    document.querySelectorAll('.filter-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
      });
    });
  </script>
</BaseLayout>