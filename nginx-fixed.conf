server {
    listen 80;
    server_name new.theprofitplatform.com.au;

    root /home/avi/projects/astro-site;
    index index.html index.htm;

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Specific rewrite rules for .html extensions
    rewrite ^/contact\.html$ /contact/ permanent;
    rewrite ^/about\.html$ /about/ permanent;
    rewrite ^/portfolio\.html$ /portfolio/ permanent;
    rewrite ^/pricing\.html$ /pricing/ permanent;
    rewrite ^/services\.html$ /services/ permanent;
    rewrite ^/free-audit\.html$ /free-audit/ permanent;
    rewrite ^/seo\.html$ /seo/ permanent;
    rewrite ^/google-ads\.html$ /google-ads/ permanent;
    rewrite ^/web-design\.html$ /web-design/ permanent;

    # Main location
    location / {
        try_files $uri $uri/ $uri/index.html =404;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    location = /50x.html {
        root /usr/share/nginx/html;
    }

    # Logging
    access_log /var/log/nginx/new.theprofitplatform.com.au.access.log;
    error_log /var/log/nginx/new.theprofitplatform.com.au.error.log;
}